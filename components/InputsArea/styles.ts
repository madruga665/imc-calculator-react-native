import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    height: 200,
    alignSelf: "stretch",
    justifyContent: "space-around",
    alignItems: "center",
    // borderWidth: 1,
    // borderColor: "#c0392b",
    paddingVertical: 30,
  },
  input: {
    width: 300,
    height: 40,
    color: "#ecf0f1",
    borderWidth: 1,
    borderColor: "#ecf0f1",
    borderRadius: 10,
    padding: 10,
  },
});
